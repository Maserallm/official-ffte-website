import React from "react"
import { Link } from "gatsby"

const Navbar = () => {
  return (
    <ul>
      <li>
        <Link>This is the navigation bar</Link>
      </li>
    </ul>
  )
}

export default Navbar
